[package]
name = "igra-cli"
version = "0.15.0"
edition = "2021"
authors = ["Merlin"]
description = "Terminal-based management CLI for KASPA L2 nodes"
license = "MIT"
repository = "https://github.com/Zorglub4242/Igra-mgt"
homepage = "https://github.com/Zorglub4242/Igra-mgt"
documentation = "https://github.com/Zorglub4242/Igra-mgt#readme"
keywords = ["igra", "kaspa", "blockchain", "tui", "cli"]
categories = ["command-line-utilities", "development-tools"]
readme = "README.md"
build = "build.rs"

[lib]
name = "igra_cli"
path = "src/lib.rs"

[[bin]]
name = "igra-cli"
path = "src/main.rs"

# Test binaries commented out - uncomment when needed
# [[bin]]
# name = "test_wallet_grpc"
# path = "test_wallet_grpc.rs"

# [[bin]]
# name = "test_balance_only"
# path = "test_balance_only.rs"

# [[bin]]
# name = "kaswallet-cli"
# path = "kaswallet_cli.rs"

# [[bin]]
# name = "test_utxo"
# path = "test_utxo.rs"

[dependencies]
# TUI Framework
ratatui = "0.26"
crossterm = "0.27"

# Docker Integration
bollard = "0.16"

# Async Runtime
tokio = { version = "1", features = ["full"] }
futures = "0.3"

# Configuration & Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.8"
dotenv = "0.15"

# CLI
clap = { version = "4", features = ["derive", "cargo"] }

# HTTP/RPC Testing
reqwest = { version = "0.11", features = ["json"] }

# HTTP Server (for API mode)
axum = { version = "0.8", features = ["ws"], optional = true }
axum-server = { version = "0.6", features = ["tls-rustls"], optional = true }
axum-login = { version = "0.18", optional = true }  # Session-based authentication
tower = { version = "0.4", optional = true }
tower-http = { version = "0.6", features = ["cors", "fs", "trace"], optional = true }
tower-sessions = { version = "0.14", optional = true }  # Session management
tower-sessions-memory-store = { version = "0.14", optional = true }  # In-memory session store
tower-sessions-file-based-store = { version = "0.1", optional = true }  # File-based session store
hyper = { version = "1.0", optional = true }
rust-embed = { version = "8.0", features = ["include-exclude"] }  # Used for Web UI and embedded plugins
mime_guess = { version = "2.0", optional = true }

# Ethereum RPC client for L2 monitoring
ethers = { version = "2.0", features = ["ws", "rustls"] }

# gRPC for kaswallet-daemon integration
tonic = "0.11"
prost = "0.12"

# Kaspad wRPC client for transaction history
kaspa-wrpc-client = { git = "https://github.com/kaspanet/rusty-kaspa", rev = "08018e7925830c7d8fe8bc1185782eb53dab3da6" }
kaspa-rpc-core = { git = "https://github.com/kaspanet/rusty-kaspa", rev = "08018e7925830c7d8fe8bc1185782eb53dab3da6" }
kaspa-addresses = { git = "https://github.com/kaspanet/rusty-kaspa", rev = "08018e7925830c7d8fe8bc1185782eb53dab3da6" }

# Error Handling
anyhow = "1.0"
thiserror = "1.0"

# Date/Time
chrono = { version = "0.4", features = ["serde"] }
humantime = "2.1"

# Additional Utilities
regex = "1.10"
colored = "2.1"
dirs = "5.0"
indicatif = "0.17"

# System
sysinfo = "0.30"
rand = "0.8"

# Version Comparison
semver = "1.0"

# Authentication & Security
password-hash = { version = "0.5", features = ["std"], optional = true }
argon2 = { version = "0.5", optional = true }
ipnetwork = "0.20"  # IP/CIDR parsing for allowlisting
async-trait = "0.1"  # Required for axum-login traits

# Windows Service (Windows only)
[target.'cfg(windows)'.dependencies]
windows-service = "0.7"

[dev-dependencies]
tempfile = "3.10"
mockall = "0.12"

[build-dependencies]
tonic-build = "0.11"
chrono = "0.4"

[features]
default = ["tui", "server"]
tui = []      # TUI mode with ratatui
server = ["axum", "axum-server", "axum-login", "tower", "tower-http", "tower-sessions", "tower-sessions-memory-store", "tower-sessions-file-based-store", "hyper", "mime_guess", "password-hash", "argon2"]  # HTTP API server mode (rust-embed always available)

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
